node_modules/
client/node_modules/
client/build/
database/*.db
.env

# Firebase service account key (NEVER commit this)
server/firebase/serviceAccountKey.json

# Byte-compiled / optimized / DLL files
__pycache__/
*.pyc


# venv
.venv

# env keys 
.env

# Python ML — per-user trained models and runtime state (generated at runtime, not source)
backend/src/burnout_prediction/models/
*.pkl

# Test and demo artifacts (development only, not production code)
test_*.py
run_scenarios.py

# Excel outputs (generated reports, not source)
*.xlsx
*.xls
~$*.xlsx
~$*.xls

# Python cache
*.pyo
*.pyd
